<GridLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"

        android:useDefaultMargins="true"
        android:alignmentMode="alignBounds"
        android:columnOrderPreserved="false"

        android:columnCount="2"
        tools:context=".MainActivity$PlaceholderFragment"
        >

    <TextView
        android:layout_gravity="right"
        android:text="Device ID :"/>

    <TextView android:id="@+id/device_id"
        android:layout_gravity="left"/>

    <TextView
        android:layout_gravity="right"
        android:text="Furniture type :"/>

    <TextView android:id="@+id/furniture_type"
        android:layout_gravity="left"/>

    <TextView
        android:layout_gravity="right"
        android:text="Platform environment :"/>

    <TextView android:id="@+id/platform_env"
        android:layout_gravity="left"/>

    <TextView
        android:layout_gravity="right"
        android:text="OS version :"/>

    <TextView android:id="@+id/os_version"
        android:layout_gravity="left"/>

    <TextView
        android:layout_gravity="right"
        android:text="Hardware :"/>

    <TextView android:id="@+id/hardware_name"
        android:layout_gravity="left"/>

</GridLayout>
